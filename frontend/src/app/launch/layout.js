export const metadata = {
  title: "Launch Token",
  description: "Coming soon: launch tokens using x402 payments in ARENA.",
};

export default function LaunchLayout({ children }) {
  return children;
}

